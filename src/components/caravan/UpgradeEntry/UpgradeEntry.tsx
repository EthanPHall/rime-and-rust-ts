import React, { FC } from 'react';
import './UpgradeEntry.css';

interface UpgradeEntryProps {}

const UpgradeEntry: FC<UpgradeEntryProps> = () => (
  <div className="UpgradeEntry" data-testid="UpgradeEntry">
    UpgradeEntry Component
  </div>
);

export default UpgradeEntry;
